# SpecGuard

A VSCode extension that turns a plain-English spec into typed, EARS-formatted
constraints, hands code generation to your own agent (bring-your-own-agent),
then generates and runs one test per constraint in an isolated git worktree
and scores the result 0-100 — blocking a task as "done" until it passes a
quality gate.

Full documentation will land in Phase 7.
